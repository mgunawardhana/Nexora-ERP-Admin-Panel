import { FuseRouteConfigsType } from '@fuse/utils/FuseUtils';
import VehicleManagementConfig from './VehicleManagementConfig';

const VehicleManagementRoot: FuseRouteConfigsType = [
	VehicleManagementConfig
];

export default VehicleManagementRoot;